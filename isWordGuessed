secretWord = str(input("Enter a secret word: "))
lettersGuessed = input("Guess the letters: ")
def isWordGuessed (secretWord, lettersGuessed):
        for f in secretWord:
            s = f in lettersGuessed
            if f == ("False"):
                break
            return s
print (isWordGuessed(secretWord, lettersGuessed))
